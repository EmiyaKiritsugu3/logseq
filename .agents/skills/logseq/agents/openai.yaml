interface:
  display_name: "Logseq"
  short_description: "Repo-specific patterns and workflows for logseq"
  default_prompt: "Use the logseq repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true